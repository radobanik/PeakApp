export type SvgIconProps = {
  size?: string
  stroke?: string
  strokeWidth?: string
}

export const DEFAULT_SVG_ICON_PROPS: SvgIconProps = {
  size: '100%',
  stroke: 'var(--foreground)',
  strokeWidth: '1.5',
}
