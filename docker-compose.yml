version: '3.8'

services:
  db:
    container_name: peak_app_db
    build:
      context: ./backend/db
      dockerfile: Dockerfile
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
