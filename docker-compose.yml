services:
  db:
    image: postgres:latest
    container_name: peak_app_db
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - ./apps/backend/.env

volumes:
  postgres_data:
